import styled from 'styled-components';

const ProjectsContainer = styled.div`
	padding: 12rem 6rem;
	background: #f5bbda;
`;

const ProjectsTitleArea = styled.div`
	display: flex;
	gap: 2rem;
	margin-bottom: 3rem;
`;

const ProjectsTitle = styled.h1`
	font-size: 5rem;
	font-weight: bold;
`;

const S = { ProjectsContainer, ProjectsTitleArea, ProjectsTitle };

export default S;
